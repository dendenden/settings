;; init-key-bindings.el
;;----------------------------------------------------------------------------
;; Key bindings
;;----------------------------------------------------------------------------

;(global-set-key (kbd "C-x /") 'shell-command) ;; run shell
; или вообще применить клавишу super как meta
(setq x-super-keysym 'meta) ;; set meta key
(setq meta 'ctrl) ;; set ctrl - alt


(provide 'init-key-bindings)

